# Streamlit_Hierarchical_Clustering_App
In this repository I have uploaded streamlit deployable code to display the hierarchical clustering using dendogram to know the no of clusters and displayed the silhouette score as model accuracy
